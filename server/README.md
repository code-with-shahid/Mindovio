# Mindovio Server

Backend API for **Mindovio** — AI Powered Smart Study Assistant.

## Scripts

- `npm run dev` — start with nodemon
- `npm start` — production start

## Env

See the root [README](../README.md) for required environment variables (`MONGODB_URL`, `JWT_SECRET`, `GEMINI_API_KEY`, Stripe, `CLIENT_URL`).
